version: "3.1"

services:
  # garbage-api:
  #   build:
  #     context: ./
  #     dockerfile: Dockerfile
  #   restart: always
  #   ports:
  #     - 5000:5000
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - 5432:5432
    volumes:
      - "db:/var/lib/postgresql/data"

volumes:
  db:
